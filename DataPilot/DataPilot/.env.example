# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/datapilot"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-here"

# NextAuth Configuration
NEXTAUTH_SECRET="your-nextauth-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Environment
NODE_ENV="production"

# Optional: OpenRouter API Key for AI features
OPENROUTER_API_KEY="your-openrouter-api-key-here"

# Railway Deployment Notes:
# 1. Set DATABASE_URL to your Railway PostgreSQL connection string
# 2. Generate a strong JWT_SECRET (32+ characters)
# 3. Generate a strong NEXTAUTH_SECRET (32+ characters)
# 4. Set NEXTAUTH_URL to your Railway deployment URL
# 5. Set NODE_ENV to "production"
# 6. OPENROUTER_API_KEY is optional for AI features